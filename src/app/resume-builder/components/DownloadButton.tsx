"use client";
import { Button } from "@heroui/react";
import React from "react";

const DownloadButton = () => {
  return <Button variant="faded">Download</Button>;
};

export default DownloadButton;
